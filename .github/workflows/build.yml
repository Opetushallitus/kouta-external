name: Kouta-external

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '**.md'

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@a37ce9120846195fa4ece8f58b268e6043cb2f26 # v3

      - name: Set up JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5
        with:
          java-version: '21'
          distribution: 'corretto'
          cache: 'maven'

      - uses: szenius/set-timezone@d1416e74a9a643cbf6c711905741e73e3a0c26b8 # v1.1
        with:
          timezoneLinux: "Europe/Helsinki"

      - name: Set up build prerequisites
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          git clone https://github.com/Opetushallitus/ci-tools.git
          source ci-tools/common/setup-tools.sh
          (cd postgresql/docker && docker build --tag koutaexternal-postgres .)
          sudo apt install docker-compose

      - name: Build with Maven
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mvn --settings settings.xml clean package -B

      - uses: actions/cache@6f8efc29b200d32929f49075959781ed54ec270c # v3
        id: restore-build
        with:
          path: |
            kouta-external/target
            europass-publisher/target
            ovara-kouta-light/target
          key: ${{ github.sha }}

  deploy-kouta-container:
    needs: test-and-build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@a37ce9120846195fa4ece8f58b268e6043cb2f26 # v3

      - uses: actions/cache@6f8efc29b200d32929f49075959781ed54ec270c # v3
        id: restore-build
        with:
          path: |
            kouta-external/target
            europass-publisher/target
            ovara-kouta-light/target
          key: ${{ github.sha }}

      - name: Build and deploy Docker container
        shell: bash
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          git clone https://github.com/Opetushallitus/ci-tools.git
          source ci-tools/common/setup-tools.sh
          export BASE_IMAGE="baseimage-fatjar-openjdk21:master"
          export KOUTA_ARTIFACT_NAME="kouta-external"

          mv kouta-external/target/kouta-external-*-jar-with-dependencies.jar \
            $DOCKER_BUILD_DIR/artifact/${KOUTA_ARTIFACT_NAME}.jar
          cp -vr kouta-external/src/main/resources/* $DOCKER_BUILD_DIR/config/
          ./ci-tools/common/pull-image.sh
          ./ci-tools/github-build/build-fatjar.sh $KOUTA_ARTIFACT_NAME
          ./ci-tools/github-build/upload-image.sh $KOUTA_ARTIFACT_NAME

  deploy-europass-container:
    needs: test-and-build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@a37ce9120846195fa4ece8f58b268e6043cb2f26 # v3

      - uses: actions/cache@6f8efc29b200d32929f49075959781ed54ec270c # v3
        id: restore-build
        with:
          path: |
            kouta-external/target
            europass-publisher/target
            ovara-kouta-light/target
          key: ${{ github.sha }}

      - name: Build and deploy Docker container
        shell: bash
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          git clone https://github.com/Opetushallitus/ci-tools.git
          source ci-tools/common/setup-tools.sh
          export BASE_IMAGE="baseimage-fatjar-openjdk21:master"
          export EUROPASS_ARTIFACT_NAME="kouta-external-europass"

          mv europass-publisher/target/europass-publisher-*-jar-with-dependencies.jar \
            $DOCKER_BUILD_DIR/artifact/${EUROPASS_ARTIFACT_NAME}.jar
          cp -vr europass-publisher/src/main/resources/* $DOCKER_BUILD_DIR/config/
          ./ci-tools/common/pull-image.sh
          ./ci-tools/github-build/build-fatjar.sh $EUROPASS_ARTIFACT_NAME
          ./ci-tools/github-build/upload-image.sh $EUROPASS_ARTIFACT_NAME

  deploy-ovara-kouta-light-container:
    needs: test-and-build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@a37ce9120846195fa4ece8f58b268e6043cb2f26 # v3

      - uses: actions/cache@6f8efc29b200d32929f49075959781ed54ec270c # v3
        id: restore-build
        with:
          path: |
            kouta-external/target
            europass-publisher/target
            ovara-kouta-light/target
          key: ${{ github.sha }}

      - name: Build and deploy Docker container
        shell: bash
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          git clone https://github.com/Opetushallitus/ci-tools.git
          source ci-tools/common/setup-tools.sh
          export BASE_IMAGE="baseimage-fatjar-openjdk21:master"
          export OVARA_KOUTA_LIGHT_ARTIFACT_NAME="ovara-kouta-external-kouta-light"

          mv ovara-kouta-light/target/ovara-kouta-light-*-jar-with-dependencies.jar \
            $DOCKER_BUILD_DIR/artifact/${OVARA_KOUTA_LIGHT_ARTIFACT_NAME}.jar
          cp -vr ovara-kouta-light/src/main/resources/* $DOCKER_BUILD_DIR/config/
          ./ci-tools/common/pull-image.sh
          ./ci-tools/github-build/build-fatjar.sh $OVARA_KOUTA_LIGHT_ARTIFACT_NAME
          ./ci-tools/github-build/upload-image.sh $OVARA_KOUTA_LIGHT_ARTIFACT_NAME
